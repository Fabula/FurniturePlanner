package model
{
	public class RoomSize
	{
		
		public var width:Number;
		public var length:Number;
		public var height:Number;
		
		public function RoomSize(width:Number, length:Number, height:Number)
		{
			this.width = width;
			this.length = length;
			this.height = height;
		}
	}
}